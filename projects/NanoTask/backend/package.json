{
  "name": "nanotask-gateway",
  "version": "1.0.0",
  "type": "module",
  "description": "NanoTask Gateway — minimalist escrow demo (zero deps)",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": { "node": ">=18" }
}
